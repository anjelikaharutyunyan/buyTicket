import { createTheme } from "@mui/material";

//main color of menu
export const theme = createTheme({
    palette: {
        primary: {
            main: "#F9BE32"
        }
    }
});

export const TICKETS_PER_PAGE = 4;